---
phase: 13-results-ranking-display
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Auto-stop triggers when TOP_RESULTS_COUNT results have RS=0"
    - "Perfect result definition is RS=0 (not pixel diff=0)"
    - "Results with RS=0 but pixel diff>0 are still counted as perfect"
  artifacts:
    - path: "index.html"
      provides: "Updated checkAutoStopCondition function"
      contains: "rsCorrections === 0"
  key_links:
    - from: "checkAutoStopCondition"
      to: "search auto-stop"
      via: "handleWorkerProgress/handleWorkerComplete"
      pattern: "checkAutoStopCondition.*rsCorrections"
---

<objective>
Redefine "perfect result" from pixel diff=0 to RS=0 for auto-stop triggering.

Purpose: RS corrections=0 means the QR scans with 100% reliability margin, which is the true measure of quality. A result with RS=0 but pixel diff>0 is actually better than RS>0 with pixel diff=0 because it proves the QR scanner had no correction burden.

Output: Auto-stop triggers when TOP_RESULTS_COUNT results with RS=0 are found, regardless of pixel diff values.
</objective>

<execution_context>
@/home/node/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/node/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-rs-measurement/12-02-SUMMARY.md

Key implementation points:
- checkAutoStopCondition function: index.html lines 15739-15746
- Current condition: `r.decodable && r.pixelDiff === 0` (line 15744)
- rsCorrections field: Infinity for non-decodable, integer >= 0 for decodable
- TOP_RESULTS_COUNT constant: line 13793
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update checkAutoStopCondition to use RS=0 as perfect criterion</name>
  <files>index.html</files>
  <action>
Modify checkAutoStopCondition (around line 15739-15746) to redefine "perfect result":

1. Change the perfect count filter from:
   `r.decodable && r.pixelDiff === 0`
   to:
   `r.decodable && r.rsCorrections === 0`

2. Update the JSDoc comment (line 15737) from:
   "@returns {boolean} True if TOP_RESULTS_COUNT results have 0 pixel errors AND are decodable"
   to:
   "@returns {boolean} True if TOP_RESULTS_COUNT results have RS=0 (perfect QR reliability)"

This change means:
- A result with RS=0, pixelDiff=5 IS counted as perfect (QR scans perfectly)
- A result with RS=3, pixelDiff=0 is NOT counted as perfect (QR needs correction)
  </action>
  <verify>
Search code for "rsCorrections === 0" in checkAutoStopCondition. Verify the condition matches: `r.decodable && r.rsCorrections === 0`.
  </verify>
  <done>
checkAutoStopCondition uses RS=0 as the perfect result criterion, not pixel diff=0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update auto-stop log message to reference RS instead of pixel errors</name>
  <files>index.html</files>
  <action>
Update the auto-stop console log message (around line 15506) to reflect the new criterion:

Change from:
`Auto-stop triggered: ${TOP_RESULTS_COUNT} perfect results found`

To:
`Auto-stop triggered: ${TOP_RESULTS_COUNT} results with RS=0 found`

This makes the log message accurate and helps with debugging.
  </action>
  <verify>
Search for "Auto-stop triggered" in code, verify message mentions "RS=0".
  </verify>
  <done>
Auto-stop log message accurately describes the RS=0 condition.
  </done>
</task>

</tasks>

<verification>
1. Review checkAutoStopCondition function, confirm it checks `rsCorrections === 0`
2. Run optimization search with pattern that produces RS=0 results
3. Verify auto-stop triggers when TOP_RESULTS_COUNT RS=0 results found
4. Verify console logs "RS=0" in auto-stop message
5. Verify a result with RS=0, pixelDiff>0 is counted toward perfect count
</verification>

<success_criteria>
- Perfect result is RS=0 regardless of pixel diff (RSLT-03 complete)
- Auto-stop triggers on TOP_RESULTS_COUNT RS=0 results (RSLT-04 complete)
- Console message reflects RS=0 criterion
</success_criteria>

<output>
After completion, create `.planning/phases/13-results-ranking-display/13-02-SUMMARY.md`
</output>
