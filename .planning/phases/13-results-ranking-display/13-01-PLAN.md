---
phase: 13-results-ranking-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Result cards show RS correction count alongside pixel diff"
    - "User can see which results have fewer RS corrections at a glance"
    - "RS=0 results are visually distinguishable as best quality"
  artifacts:
    - path: "index.html"
      provides: "Result card RS display"
      contains: "rsCorrections"
  key_links:
    - from: "result.rsCorrections"
      to: "errorsSpan display"
      via: "DOM rendering in displayTopResults"
      pattern: "rsCorrections.*errors"
---

<objective>
Update result card UI to display both RS corrections and pixel diff metrics.

Purpose: Users need visibility into actual QR scan reliability (RS corrections) alongside pixel mismatch count to understand result quality.

Output: Result cards showing "RS: N | Pixels: M" format with visual indication for RS=0 (perfect) results.
</objective>

<execution_context>
@/home/node/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/node/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-rs-measurement/12-02-SUMMARY.md

Key implementation points:
- Result card rendering: index.html lines 16186-16210
- Current display: `${result.pixelDiff} errors` (line 16198)
- rsCorrections field available on result objects (from Phase 12)
- Infinity = non-decodable (should display as "-" or similar)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update result card label to show both RS and pixel metrics</name>
  <files>index.html</files>
  <action>
Modify the result card label section (around lines 16196-16202) to display both metrics:

1. Change the errorsSpan content from `${result.pixelDiff} errors` to show both RS corrections and pixel diff:
   - Format: "RS: N | Pixels: M"
   - For non-decodable results (rsCorrections === Infinity), display "RS: - | Pixels: M"

2. Add visual styling for RS=0 results:
   - When result.rsCorrections === 0, add CSS class "perfect-rs" to the errorsSpan
   - This indicates the QR scans with maximum reliability margin

Implementation detail:
```javascript
const rsDisplay = result.rsCorrections === Infinity ? "-" : result.rsCorrections;
errorsSpan.textContent = `RS: ${rsDisplay} | Pixels: ${result.pixelDiff}`;
if (result.rsCorrections === 0) {
  errorsSpan.classList.add("perfect-rs");
}
```
  </action>
  <verify>
Open index.html in browser, run optimization search, verify result cards show "RS: N | Pixels: M" format.
  </verify>
  <done>
Result cards display both RS correction count and pixel diff; RS=0 results have perfect-rs class applied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS styling for perfect-rs indicator</name>
  <files>index.html</files>
  <action>
Add CSS rule for the .perfect-rs class to visually distinguish RS=0 results:

1. Find the existing .errors CSS selector (likely in the styles section)
2. Add .perfect-rs style below it:
   - Use a distinctive color (green or similar success color)
   - Example: `.perfect-rs { color: #10b981; font-weight: 600; }`

This makes RS=0 results immediately visible as highest quality results.
  </action>
  <verify>
Open index.html in browser with results that have RS=0, verify they display with distinct styling (green text, bold).
  </verify>
  <done>
RS=0 results are visually distinct from other results via .perfect-rs styling.
  </done>
</task>

</tasks>

<verification>
1. Run optimization search on a simple pattern
2. Verify result cards show "RS: N | Pixels: M" format
3. Verify non-decodable results show "RS: - | Pixels: M"
4. Verify RS=0 results have green/bold styling
5. Verify sorting order unchanged (RS ascending, then pixel diff ascending)
</verification>

<success_criteria>
- Each result card displays both RS corrections and pixel diff
- RS=0 results are visually distinct (RSLT-01 complete)
- Format is clear and readable at a glance
</success_criteria>

<output>
After completion, create `.planning/phases/13-results-ranking-display/13-01-SUMMARY.md`
</output>
