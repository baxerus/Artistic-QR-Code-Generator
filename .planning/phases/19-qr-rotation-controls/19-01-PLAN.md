---
phase: 19-qr-rotation-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [ROT-01]

must_haves:
  truths:
    - "User can rotate the Paint Pattern QR preview in 90 degree steps without rotating painted pixels."
    - "Rotation persists across reloads and resets to 0 when Clear Pattern or version change occurs."
    - "Decode metrics refresh after rotation changes."
  artifacts:
    - path: "index.html"
      provides: "rotation state, persistence, and paint-preview rendering"
  key_links:
    - from: "index.html"
      to: "runCorruptionPipeline"
      via: "rotation change handler"
      pattern: "setQrRotation\(|runCorruptionPipeline\("
    - from: "index.html"
      to: "saveRotation"
      via: "rotation state updates"
      pattern: "saveRotation"
---

<objective>
Add manual QR rotation controls in the Paint Pattern section with persistence and decode-aware preview updates.

Purpose: Let users rotate the QR presentation without altering the painted pattern while keeping decode feedback accurate.
Output: Rotation UI, state persistence, and rotation-aware paint preview rendering in index.html.
</objective>

<execution_context>
@/home/node/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/node/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rotation state, persistence, and reset hooks</name>
  <files>index.html</files>
  <action>
Add QR rotation state with 0/90/180/270 values, storage key, and helpers to load/save/normalize rotation. Default to last used angle on load. Reset rotation to 0 when the QR version changes and when Clear Pattern runs, and persist those resets. Ensure rotation changes call runCorruptionPipeline so decode metrics update.
  </action>
  <verify>Open index.html, rotate the QR, reload, and confirm rotation persists; change version or clear pattern and confirm rotation resets to 0.</verify>
  <done>Rotation state persists across reloads and resets to 0 on version change and Clear Pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Add rotate-90 control near paint tools</name>
  <files>index.html</files>
  <action>
Add an icon-only rotate button near the paint tools in the Paint Pattern section. Each click advances 90 degrees and updates a visual indicator of the current angle. Do not add a separate reset button. Wire the control to update rotation state and trigger preview refresh.
  </action>
  <verify>Click the rotate control four times and confirm the UI cycles 0/90/180/270 and returns to 0.</verify>
  <done>Rotate control advances in 90 degree steps with icon-only state indication.</done>
</task>

<task type="auto">
  <name>Task 3: Rotate QR base and protected overlay in paint preview</name>
  <files>index.html</files>
  <action>
Update renderQRWithOverlay and the decode pipeline to use rotation-aware QR modules and function mask while keeping painted pixels unrotated. Implement a rotation mapping helper (0/90/180/270) to read QR module values and protected-mask values as if the QR were rotated, and use that for the base QR layer and protected overlay only. Ensure decode rendering uses the same rotation mapping so metrics update correctly when rotation changes.
  </action>
  <verify>Rotate the preview and confirm painted pixels remain fixed while QR modules/protected overlay rotate and decode metrics refresh.</verify>
  <done>Paint preview rotates QR + protected overlay only, with decode metrics reflecting rotation.</done>
</task>

</tasks>

<verification>
Rotate the paint preview to each angle and confirm painted pixels stay fixed, decode metrics refresh, and rotation resets on clear or version change.
</verification>

<success_criteria>
- Rotate control exists near paint tools and cycles 0/90/180/270.
- Rotation persists across reloads and resets to 0 on Clear Pattern or version change.
- Paint preview rotates QR/protected overlay only, and decode metrics update after rotation changes.
</success_criteria>

<output>
After completion, create `.planning/phases/19-qr-rotation-controls/19-01-SUMMARY.md`.
</output>
