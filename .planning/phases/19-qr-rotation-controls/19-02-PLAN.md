---
phase: 19-qr-rotation-controls
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified: [index.html]
autonomous: true
requirements: [ROT-02, ROT-03]

must_haves:
  truths:
    - "Result thumbnails and expanded overlays reflect the current QR rotation."
    - "PNG and SVG exports match the current QR rotation without rotating painted pixels."
  artifacts:
    - path: "index.html"
      provides: "rotation-aware result rendering and export"
  key_links:
    - from: "index.html"
      to: "renderResultToCanvas"
      via: "rotation-aware module mapping"
      pattern: "renderResultToCanvas\(|rotate"
    - from: "index.html"
      to: "renderErrorOverlay"
      via: "rotation-aware overlay mapping"
      pattern: "renderErrorOverlay\(|rotate"
    - from: "index.html"
      to: "generateSVG"
      via: "rotation-aware SVG path"
      pattern: "generateSVG\(" 
---

<objective>
Rotate result previews, overlays, and exports to match the current QR rotation while keeping painted pixels orientation fixed.

Purpose: Ensure all result displays and exported assets align with the rotated QR presentation.
Output: Rotation-aware result rendering and export handling in index.html.
</objective>

<execution_context>
@/home/node/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/node/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rotate result preview canvases</name>
  <files>index.html</files>
  <action>
Update result rendering so the QR preview canvas uses rotation-aware module data, matching the current rotation. Use the same rotation mapping helper from plan 19-01 to render module data in rotated orientation without changing painted pixels or stored data.
  </action>
  <verify>Change rotation and confirm result thumbnails rotate accordingly.</verify>
  <done>Result preview canvases reflect current rotation.</done>
</task>

<task type="auto">
  <name>Task 2: Rotate hover overlays for results</name>
  <files>index.html</files>
  <action>
Update renderErrorOverlay to read rotated QR module data and function mask while keeping painted pixels unrotated. Ensure both collapsed and expanded overlays render with rotation-aware mapping and remain aligned with the rotated preview canvas.
  </action>
  <verify>Hover/click result cards at different rotations and confirm overlays align with rotated QR previews.</verify>
  <done>Result overlays rotate with QR previews and remain aligned.</done>
</task>

<task type="auto">
  <name>Task 3: Align PNG and SVG exports with rotation</name>
  <files>index.html</files>
  <action>
Update export helpers to apply rotation when generating PNG and SVG outputs. PNG should export the rotated QR module data. SVG should build the path based on rotated module data. Ensure exports still use the same filename and content semantics, only rotated.
  </action>
  <verify>Export PNG/SVG at different rotations and confirm the rotated output matches the preview.</verify>
  <done>Exports match current rotation while leaving painted pixels orientation unchanged.</done>
</task>

</tasks>

<verification>
Generate results, switch rotations, and validate that thumbnails, overlays, and PNG/SVG exports match the rotated QR presentation.
</verification>

<success_criteria>
- Result previews and hover overlays reflect the current rotation.
- PNG/SVG exports match the current rotation.
</success_criteria>

<output>
After completion, create `.planning/phases/19-qr-rotation-controls/19-02-SUMMARY.md`.
</output>
