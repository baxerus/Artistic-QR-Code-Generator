---
phase: 19-qr-rotation-controls
plan: 03
type: execute
wave: 3
depends_on: [19-01]
files_modified: [index.html]
autonomous: true
requirements: [ROT-04]

must_haves:
  truths:
    - "User can enable random rotation for Generate and each attempt can use a different angle."
    - "Random rotation overrides manual rotation without showing the angle."
    - "Result previews reflect the rotation used during search."
  artifacts:
    - path: "index.html"
      provides: "random rotation UI/state and worker integration"
  key_links:
    - from: "index.html"
      to: "startOptimization"
      via: "rotation settings in worker message"
      pattern: "START_OPTIMIZATION|rotation"
---

<objective>
Add random rotation mode to Generate so optimization applies 0/90/180/270 rotations per attempt without exposing the chosen angle.

Purpose: Broaden search space while keeping manual rotation separate and hidden during random mode.
Output: Random rotation UI, state, and worker integration in index.html.
</objective>

<execution_context>
@/home/node/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/node/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add random rotation control in Generate section</name>
  <files>index.html</files>
  <action>
Add a "Random rotation" toggle near the Generate controls. Store its state locally (persisted) and ensure it overrides manual rotation selection while enabled. Do not display the chosen random angle anywhere in the UI.
  </action>
  <verify>Enable/disable random rotation and confirm the manual rotation control is overridden while random is on.</verify>
  <done>Random rotation toggle persists and overrides manual rotation while enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Apply random rotation per attempt in worker</name>
  <files>index.html</files>
  <action>
Extend the worker message protocol to include rotation mode and base rotation. When random rotation is enabled, choose a random 0/90/180/270 rotation for each candidate attempt and apply it to QR module reads and function mask checks in the worker. Ensure result payloads include the rotation used so previews can match that rotation later.
  </action>
  <verify>Run optimization with random rotation enabled and confirm results include rotation metadata and previews match the rotated output.</verify>
  <done>Worker applies per-attempt random rotation and returns rotation in result objects.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate rotation metadata into result rendering</name>
  <files>index.html</files>
  <action>
Update top-results rendering to use each result's stored rotation (from worker) rather than the global rotation when random mode is enabled. Preserve manual rotation behavior when random is off.
  </action>
  <verify>Toggle random rotation and confirm results reflect per-result rotation while manual mode uses the global rotation.</verify>
  <done>Result rendering respects per-result rotation in random mode.</done>
</task>

</tasks>

<verification>
Enable random rotation, run Generate, and verify results preview rotations vary without exposing the random angle in the UI.
</verification>

<success_criteria>
- Random rotation option exists and overrides manual rotation while enabled.
- Each attempt can use a different rotation; results reflect per-result rotation metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/19-qr-rotation-controls/19-03-SUMMARY.md`.
</output>
