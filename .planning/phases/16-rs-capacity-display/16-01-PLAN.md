---
phase: 16-rs-capacity-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [RSCAP-01, RSCAP-02]

must_haves:
  truths:
    - "Result cards show 'Corrections: X of Y' with integer values and placeholders when data is missing"
    - "Hovering the corrections line explains both current corrections and the max capacity"
    - "Max capacity (Y) matches the QR version's level H RS capacity from jsQR VERSIONS"
  artifacts:
    - path: "index.html"
      provides: "Global access to jsQR VERSIONS and RS capacity helper"
      contains: "__JSQR_VERSIONS__"
    - path: "index.html"
      provides: "Updated corrections display in result cards"
      contains: "Corrections: <b>"
  key_links:
    - from: "jsQR VERSIONS module"
      to: "window.__JSQR_VERSIONS__"
      via: "global assignment"
      pattern: "__JSQR_VERSIONS__ = exports.VERSIONS"
    - from: "updateTopResults"
      to: "getMaxCorrectionsCapacity"
      via: "computed display value"
      pattern: "getMaxCorrectionsCapacity\(currentVersion\)"
---

<objective>
Display Reed-Solomon correction capacity alongside current corrections so users can see how close a QR code is to scan failure.

Purpose: Provide clear headroom context (X of Y) using jsQR's authoritative version metadata.
Output: Global exposure of jsQR VERSIONS + capacity helper + updated result card copy/tooltip.
</objective>

<execution_context>
@/home/node/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/node/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-rs-capacity-display/16-CONTEXT.md
@.planning/phases/16-rs-capacity-display/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose jsQR VERSIONS and compute RS capacity</name>
  <files>index.html</files>
  <action>
Expose the jsQR VERSIONS table to app scope and add a helper that computes max RS corrections for level H.

1. In the inlined jsQR bundle, locate the module that defines `exports.VERSIONS = [`.
   - After the `exports.VERSIONS = [...]` array literal closes, add a single global assignment:
     - `if (typeof window !== "undefined") window.__JSQR_VERSIONS__ = exports.VERSIONS;`
     - Also allow worker/self contexts: `else if (typeof self !== "undefined") self.__JSQR_VERSIONS__ = exports.VERSIONS;`
   - Keep this minimal to avoid altering jsQR logic.

2. Near other utility helpers (around the SVG/PNG helpers), add:
   - `function getMaxCorrectionsCapacity(versionNumber) { ... }`
   - Behavior:
     - Read `const versions = window.__JSQR_VERSIONS__` (or `self.__JSQR_VERSIONS__` if window undefined).
     - If missing or invalid, return `null`.
     - Use `versions[versionNumber - 1]` (1-based version lookup).
     - Use error correction level H index `3`.
     - Compute `totalEcCodewords = sum(ecBlocks[i].numBlocks * ecCodewordsPerBlock)`.
     - Return `Math.floor(totalEcCodewords / 2)` if > 0, otherwise `null`.
   - Keep values integer-only; no decimals.
  </action>
  <verify>
Search index.html for `__JSQR_VERSIONS__` and confirm it is assigned from `exports.VERSIONS` inside the jsQR bundle. Search for `function getMaxCorrectionsCapacity` and verify it:
1) uses `versionNumber - 1`,
2) uses errorCorrectionLevels[3],
3) sums `numBlocks * ecCodewordsPerBlock`,
4) divides by 2 and returns integer or null for invalid values.
  </verify>
  <done>
`window.__JSQR_VERSIONS__` (or `self.__JSQR_VERSIONS__`) exists after jsQR loads, and `getMaxCorrectionsCapacity(version)` returns a positive integer or null for invalid/missing data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update result card corrections display and tooltip</name>
  <files>index.html</files>
  <action>
Update the correction line in `updateTopResults` to the exact format "Corrections: X of Y" with placeholder rules and extended tooltip. Also ensure any other occurrences of the "Corrections:" label in index.html use the same "X of Y" format.

1. Search index.html for every "Corrections:" occurrence and update any other display locations to the same "Corrections: X of Y" format with placeholders and tooltip rules (if `updateTopResults` is the only place, explicitly confirm no other occurrences need changes).

2. Replace the existing corrections display logic (currently `Corrections: <b>${rsDisplay}</b>`) with:
   - `const rsValue = Number.isFinite(result.rsCorrections) ? result.rsCorrections : null;`
   - `const maxCapacity = getMaxCorrectionsCapacity(currentVersion);`
   - `const rsDisplay = rsValue === null ? "-" : rsValue;`
   - `const maxDisplay = maxCapacity && maxCapacity > 0 ? maxCapacity : "-";`
   - `correctionSpan.innerHTML = \`Corrections: <b>${rsDisplay}</b> of <b>${maxDisplay}</b>\`;`

3. Extend `correctionSpan.title` to explain the max value (Y):
   - Keep the existing message about what corrections mean.
   - Add a sentence explaining that the max is the correction capacity for the QR version at level H.
   - Keep the tooltip even when placeholders are shown.

4. Ensure Corrections appears before Pixels (keep existing ordering).
  </action>
  <verify>
In index.html:
1) Every "Corrections:" occurrence uses the "Corrections: <b>X</b> of <b>Y</b>" format (or "-" placeholders).
2) Placeholder "-" appears when rsCorrections is missing/Infinity or when capacity is unavailable.
3) Tooltip text includes the max-capacity explanation.
  </verify>
  <done>
Result cards display "Corrections: X of Y" with integer values, use "-" placeholders when needed, and the tooltip explains both current corrections and the max capacity.
  </done>
</task>

</tasks>

<verification>
1. Open index.html and run a search to generate results.
2. Confirm each result shows "Corrections: X of Y" and "Pixel errors" remains below it.
3. Hover the corrections line to see the updated tooltip explaining the max value.
4. (Optional) Temporarily force missing RS or capacity values to confirm placeholders display as "-".
</verification>

<success_criteria>
- Result cards show "Corrections: X of Y" with integer values and placeholders for missing X or Y.
- Max capacity (Y) is derived from jsQR VERSIONS using the required formula and level H.
- Corrections tooltip explains what X is and what Y represents.
</success_criteria>

<output>
After completion, create `.planning/phases/16-rs-capacity-display/16-01-SUMMARY.md`
</output>
