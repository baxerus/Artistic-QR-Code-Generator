---
phase: 10-visual-polish-result-inspection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Blue protected borders are clearly visible on paint canvas (opacity ~0.25)"
    - "Overlay transition is immediate (0.1s, no delay)"
    - "Legend only shows in expanded view, not on small result hover"
    - "Expanded results show overlay only on hover, not always"
    - "Blue dashed borders are visible (thicker line, larger dash)"
  artifacts:
    - path: "index.html"
      provides: "Fixed CSS rules and canvas rendering"
      contains: "rgba(59, 130, 246, 0.25)"
  key_links:
    - from: ".result-slot.expanded:hover"
      to: ".result-overlay opacity"
      via: "CSS hover rule"
      pattern: "\\.expanded:hover.*\\.result-overlay"
---

<objective>
Close 4 UAT gaps from user testing of visual polish features.

Purpose: Fix visibility and interaction issues reported during UAT
Output: Corrected overlay behavior matching user expectations
</objective>

<execution_context>
@/home/node/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/node/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-polish-result-inspection/10-UAT.md
@.planning/phases/10-visual-polish-result-inspection/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix blue overlay visibility and transition timing</name>
  <files>index.html</files>
  <action>
Fix 3 CSS/constant issues:

1. **Increase blue overlay opacity** (line ~13590):
   - Change `PROTECTED_OVERLAY_COLOR` from `rgba(59, 130, 246, 0.15)` to `rgba(59, 130, 246, 0.25)`
   - User reported blue was too transparent compared to the previous red

2. **Speed up overlay transition** (line ~716):
   - Change `transition: opacity 0.3s ease 0.3s;` to `transition: opacity 0.1s ease;`
   - Remove the 0.3s delay entirely, reduce duration to 0.1s for immediate feel

3. **Remove legend on non-expanded hover** (lines ~727-729):
   - Delete the rule `.result-slot:hover .overlay-legend { display: flex; }`
   - Keep the existing `.result-slot.expanded .overlay-legend { display: flex; }` rule (lines ~771-773)
   - This ensures legend only appears in expanded view, preventing layout jump on small result hover
  </action>
  <verify>
    - Grep for "0.25)" to confirm opacity increase
    - Grep for "0.1s ease;" to confirm fast transition
    - Grep shows no ".result-slot:hover .overlay-legend" rule
  </verify>
  <done>
    - Blue overlay more visible (0.25 opacity)
    - Transition feels immediate (0.1s, no delay)
    - Legend only shows in expanded view
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix expanded result overlay hover behavior and dashed border visibility</name>
  <files>index.html</files>
  <action>
Fix 2 issues with expanded results:

1. **Make expanded overlay require hover** (lines ~719-721):
   - Change `.result-slot.expanded .result-overlay { opacity: 1; }` 
   - To `.result-slot.expanded:hover .result-overlay { opacity: 1; }`
   - This ensures overlay hides when mouse leaves expanded result (same as small results)

2. **Increase dashed border visibility** (lines ~15903-15904):
   - Change `ctx.lineWidth = 0.15;` to `ctx.lineWidth = 0.5;`
   - Change `ctx.setLineDash([0.5, 0.25]);` to `ctx.setLineDash([2, 1]);`
   - The current values are too thin/small to be visible even at module scale
   - These larger values will create visible dashed outlines around protected areas
  </action>
  <verify>
    - Grep for ".expanded:hover .result-overlay" confirms hover requirement added
    - Grep for "lineWidth = 0.5" confirms thicker line
    - Grep for "setLineDash([2, 1])" confirms larger dash pattern
  </verify>
  <done>
    - Expanded results show overlay only when hovering
    - Moving mouse away hides overlay in expanded view
    - Blue dashed borders are clearly visible around protected areas
  </done>
</task>

</tasks>

<verification>
Run app and verify all 4 UAT issues are fixed:
1. Blue borders on paint canvas are clearly visible (not too transparent)
2. Overlay appears immediately on hover (no noticeable delay)
3. Small results don't show legend on hover (no layout jump)
4. Expanded results show overlay only on hover, hide on mouse leave
5. Blue dashed borders are visible around protected areas in expanded view
</verification>

<success_criteria>
- UAT Test 1 passes: Blue borders clearly visible
- UAT Test 3 passes: Immediate transition, no legend on small result hover
- UAT Test 4 passes: Blue dashed borders visible only on hover
- UAT Test 5 passes: Overlay disappears when mouse leaves expanded result
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-polish-result-inspection/10-02-SUMMARY.md`
</output>
