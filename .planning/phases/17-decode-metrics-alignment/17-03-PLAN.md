---
phase: 17-decode-metrics-alignment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [DECODE-01, DECODE-02, DECODE-03]

must_haves:
  truths:
    - "Corrections and Pixel errors each render on their own lines in the Paint Pattern decode status"
    - "Neutral state reserves the same two-line metrics area as other states, without narrowing"
    - "Decode failure shows Corrections as max capacity value of the current QR version"
  artifacts:
    - path: "index.html"
      provides: "Decode status row markup and metrics rendering"
      contains: "decode-metrics"
  key_links:
    - from: "index.html"
      to: "getMaxCorrectionsCapacity"
      via: "decode failure corrections display"
      pattern: "getMaxCorrectionsCapacity"
    - from: "index.html"
      to: "decode-metrics"
      via: "per-line metrics rendering"
      pattern: "decode-metrics"
---

<objective>
Make Paint Pattern decode metrics use separate lines, preserve neutral row width, and show max-capacity corrections on decode failure.

Purpose: Improve clarity of decode metrics and keep status row layout consistent across states.
Output: Updated decode status markup, styling, and failure rendering in `index.html`.
</objective>

<execution_context>
@/home/node/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/node/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-decode-metrics-alignment/17-CONTEXT.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split corrections and pixel errors into separate metric lines</name>
  <files>index.html</files>
  <action>
Update the decode metrics markup so corrections and pixel errors each render on their own line (two stacked rows within `.decode-metrics`). Preserve the status label line above. Keep the same labels and tooltip behavior as result cards. Ensure the neutral state keeps the metrics container in the DOM with `visibility: hidden` (not removed) so layout width/height remains consistent, and avoid using any alternate placeholder text that reduces row width.
  </action>
  <verify>Expand Paint Pattern, paint a pattern, and confirm the decode status shows three lines total: status label, corrections line, pixel errors line; neutral keeps the same reserved space without narrowing.</verify>
  <done>Corrections and Pixel errors render as separate stacked lines and neutral state preserves row sizing.</done>
</task>

<task type="auto">
  <name>Task 2: Render max-capacity corrections on decode failure</name>
  <files>index.html</files>
  <action>
Adjust `updateDecodeResult` so when decode fails (non-empty pattern, status = fail), the corrections line displays `Corrections: <MAX> of <MAX>` where `<MAX>` is `getMaxCorrectionsCapacity(currentVersion)`. Keep pixel errors as the numeric value already computed for the pattern. Do not change success behavior or neutral handling.
  </action>
  <verify>Force a decode failure and confirm the corrections line shows `Corrections: X of X` where X matches the max capacity for the current QR version (same value used in result cards).</verify>
  <done>Decode failure renders corrections using the max-capacity value rather than dashes.</done>
</task>

</tasks>

<verification>
- With a non-empty pattern, verify corrections and pixel errors appear on separate lines and the status row height/width does not shift between Success/Fail/Neutral.
- Force a failure state and confirm corrections render as `X of X` using the current version's capacity.
</verification>

<success_criteria>
- Corrections and Pixel errors are displayed on their own lines beneath the status label.
- Neutral state reserves the same metrics space without narrowing the row.
- Decode failure uses max capacity value for `Corrections: X of X`.
</success_criteria>

<output>
After completion, create `.planning/phases/17-decode-metrics-alignment/17-03-SUMMARY.md`.
</output>
