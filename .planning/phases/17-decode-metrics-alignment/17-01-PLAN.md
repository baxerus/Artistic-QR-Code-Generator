---
phase: 17-decode-metrics-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [DECODE-01, DECODE-02, DECODE-03]

must_haves:
  truths:
    - "Paint Pattern decode status shows Corrections: X of Y using the same capacity calculation as result cards"
    - "Paint Pattern decode status shows Pixel errors: Z whenever a pattern exists"
    - "Decode status row height stays fixed when switching Success/Fail/Neutral"
  artifacts:
    - path: "index.html"
      provides: "Decode status row markup, styling, and metrics update logic"
      contains: "Corrections: <b>"
  key_links:
    - from: "index.html"
      to: "getMaxCorrectionsCapacity"
      via: "decode metrics display"
      pattern: "getMaxCorrectionsCapacity"
    - from: "index.html"
      to: "debounce"
      via: "schedule decode update"
      pattern: "debounce\\("
---

<objective>
Align the Paint Pattern decode status row with result-card metrics and provide a stable two-line layout.

Purpose: Make decode feedback consistent with result cards while preventing layout jumps.
Output: Updated decode status UI and debounce-driven decode updates in `index.html`.
</objective>

<execution_context>
@/home/node/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/node/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-decode-metrics-alignment/17-CONTEXT.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure decode status row and fixed-height styling</name>
  <files>index.html</files>
  <action>
Update the Paint Pattern decode result markup to use a two-line structure: a status line and a metrics line. Replace the current `.decode-data` element with a `.decode-metrics` container that holds two inline spans for corrections and pixel errors.

Adjust CSS so `.decode-result` always reserves a fixed two-line height (label line + metrics line) with no transitions or animations on state changes. Use text-only status styling and match success/fail colors to result card status colors (`.status.decodable` / `.status.not-decodable`). Hide the metrics line in the neutral state using `visibility: hidden` (not `display: none`) so the row height does not jump. Keep neutral text in the base label color.
  </action>
  <verify>Open `index.html`, expand Paint Pattern, and toggle between empty pattern, decodable, and failed states; the row height stays fixed and the metrics line is hidden in neutral.</verify>
  <done>Decode status row renders as two lines with stable height and no icon/badge styling.</done>
</task>

<task type="auto">
  <name>Task 2: Compute and display decode corrections + pixel errors</name>
  <files>index.html</files>
  <action>
Extend `decodeCorruptedQR` to return correction count from jsQR when available (e.g., `correctionCount` on success). Add a helper to compute pixel error count for the current paint pattern by comparing painted cells against `qrModel.isDark(row, col)` for all non-unset cells; do not special-case function patterns to match result-card pixelDiff behavior.

Update `updateDecodeResult` to render metrics exactly as in result cards: `Corrections: X of Y` and `Pixel errors: Z`, with corrections first. Use `getMaxCorrectionsCapacity(currentVersion)` and copy the same tooltip text from result cards for the corrections line. On decode failure, show dashes for both values (`—`). In neutral (no pattern or decode error/exception), set label to `No pattern applied` and hide the metrics line.
  </action>
  <verify>After painting, the decode row shows `Corrections: X of Y` and `Pixel errors: Z`; on failure it shows dashes; neutral shows only the label.</verify>
  <done>Decode metrics match result-card labels, tooltips, and capacity calculation.</done>
</task>

<task type="auto">
  <name>Task 3: Auto-run decode on expand with debounce on paint changes</name>
  <files>index.html</files>
  <action>
Add a debounced decode scheduler (300–500ms; use 400ms) that triggers decode only when the Paint Pattern section is expanded. Update `togglePaintSection` to run a decode when expanding. Update `paintAt` (and clear/reset actions if needed) to call the debounced decode scheduler instead of running decode immediately, while still re-rendering the overlay for instant visual feedback. Ensure decode continues to run during optimization and remove any manual re-run affordance (none should be added).
  </action>
  <verify>With the paint section expanded, rapid painting updates decode metrics only after a short debounce; collapsing the section pauses decode updates until expanded again.</verify>
  <done>Decode auto-runs on expand and re-runs on paint changes with debounce while expanded.</done>
</task>

</tasks>

<verification>
- Expand Paint Pattern, paint several cells, and confirm the decode status shows corrections and pixel errors using the same labels as result cards.
- Force a failure case (paint heavy conflicts) and confirm metrics show dashes and status row height stays fixed.
- Collapse and re-expand Paint Pattern to confirm decode auto-runs on expand and debounced updates occur only while expanded.
</verification>

<success_criteria>
- Paint Pattern decode status displays `Corrections: X of Y` and `Pixel errors: Z` with the same capacity calculation as result cards.
- Neutral state shows `No pattern applied` with metrics hidden but row height unchanged.
- Decode updates run automatically on expand and are debounced on paint changes.
</success_criteria>

<output>
After completion, create `.planning/phases/17-decode-metrics-alignment/17-01-SUMMARY.md`.
</output>
