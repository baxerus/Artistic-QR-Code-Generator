---
phase: 17-decode-metrics-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [DECODE-02]
gap_closure: true

must_haves:
  truths:
    - "Paint Pattern decode status shows Pixel errors: Z whenever a pattern exists, even when decode fails"
  artifacts:
    - path: "index.html"
      provides: "Decode status metrics logic for pixel errors"
      contains: "Pixel errors"
  key_links:
    - from: "index.html"
      to: "decode metrics display"
      via: "pixel error calculation and render"
      pattern: "Pixel errors"
---

<objective>
Ensure Paint Pattern decode metrics show pixel error counts for any non-empty pattern, including decode failures.

Purpose: Close the DECODE-02 gap by aligning pixel error visibility with result-card behavior.
Output: Updated decode metrics calculation/rendering in `index.html`.
</objective>

<execution_context>
@/home/node/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/node/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-decode-metrics-alignment/17-CONTEXT.md
@.planning/phases/17-decode-metrics-alignment/17-VERIFICATION.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist pixel error counts for all non-empty patterns</name>
  <files>index.html</files>
  <action>
Update the decode pipeline so pixel error counts are computed for any non-empty pattern and stored on the decode result regardless of decode success. Keep corrections as-is (use dashes on decode failure per decision), and keep neutral only for empty pattern or decode errors that prevent running decode.

If the current code only assigns pixel errors on success, lift the pixel error calculation earlier (before attempting decode) and attach it to the decode result object for both success and failure paths.
  </action>
  <verify>Paint a few cells, force a decode failure, and confirm the in-memory decode result still carries a numeric pixel error count.</verify>
  <done>Pixel error counts exist on decode results for both decodable and not-decodable outcomes when a pattern exists.</done>
</task>

<task type="auto">
  <name>Task 2: Render pixel errors on failure while preserving neutral hiding</name>
  <files>index.html</files>
  <action>
Update `updateDecodeResult` to always render `Pixel errors: Z` for any non-empty pattern, even when decode fails. Keep the metrics line hidden only in Neutral (empty pattern or decode error) to preserve layout decisions. For decode failure, continue showing corrections as `— of —` but render the numeric pixel error count from the decode result.
  </action>
  <verify>With a non-empty pattern that fails decode, the metrics line shows `Corrections: — of —` and `Pixel errors: <number>`; when the pattern is empty, the metrics line remains hidden.</verify>
  <done>Pixel errors are visible whenever a pattern exists, matching result-card behavior, without breaking the neutral layout rule.</done>
</task>

</tasks>

<verification>
- Paint a small pattern that decodes successfully; confirm `Pixel errors: Z` appears and matches the rendered metrics line.
- Paint a conflicting pattern that fails decode; confirm pixel errors still show a number while corrections stay dashed.
- Clear the pattern; confirm the metrics line hides and the label reads `No pattern applied` with no layout jump.
</verification>

<success_criteria>
- Pixel error counts render for any non-empty pattern, regardless of decode success.
- Neutral state continues to hide the metrics line without affecting row height.
- DECODE-02 truth is satisfied per verification report gap.
</success_criteria>

<output>
After completion, create `.planning/phases/17-decode-metrics-alignment/17-02-SUMMARY.md`.
</output>
