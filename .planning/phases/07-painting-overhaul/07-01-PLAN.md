---
phase: 07-painting-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true

must_haves:
  truths:
    - Painted pixels render as solid color overlay on top of QR code in a single combined canvas
    - Painted white pixels are light gray (#d0d0d0), painted black pixels are dark gray (#404040)
    - Protected QR structural areas (finder/timing/alignment patterns) have border outlines
    - Cursor changes to not-allowed when hovering over protected areas
    - Grid lines are visible on hover only
    - Pattern state persists correctly after overlay rendering changes
  artifacts:
    - path: index.html
      provides: Unified QR + paint overlay canvas, protected area visualization, hover grid lines
      modifies:
        - Remove separate paint-canvas element
        - Add overlay rendering functions
        - Add protected area border rendering
        - Add hover-based grid line rendering
  key_links:
    - from: paint events
      to: overlay rendering
      via: renderQRWithOverlay function
    - from: createFunctionPatternMask
      to: protected area visualization
      via: isProtected helper function
---

<objective>
Replace the separate paint canvas with a unified QR canvas where painted pixels render as a solid color overlay. Add protected area visualization with border outlines and cursor feedback. Grid lines appear on hover only.

Purpose: Create a seamless painting experience where users paint directly on the QR code preview with clear visual distinction between painted pixels and QR content.
Output: Unified canvas rendering system with overlay, protected area outlines, and hover grid lines.
</objective>

<execution_context>
@/home/node/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/node/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-state-persistence/06-01-SUMMARY.md
@.planning/phases/07-painting-overhaul/07-CONTEXT.md
@.planning/phases/07-painting-overhaul/07-RESEARCH.md
@/workspace/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor canvas structure and rendering pipeline</name>
  <files>index.html</files>
  <action>
    Refactor the painting system from a separate canvas to overlay-based rendering on the QR canvas:

    1. Remove the separate paint-canvas element and its container from HTML (lines 731-735):
       - Remove the paint-container div wrapping the paint-canvas
       - Remove canvas id="paint-canvas"
    
    2. Modify the paint-section layout to prepare for legend positioning:
       - Keep the paint-section div
       - Structure: paint-container (will hold QR canvas + legend side-by-side)
       - Legend will be added in 07-02

    3. Create unified rendering function `renderQRWithOverlay(qrCanvas, qrModel, paintGrid, functionMask)`:
       - Clear canvas
       - Draw base QR code modules (Layer 0)
       - Draw painted pixel overlay (Layer 1):
         - Skip pixels where functionMask is true (protected areas)
         - Painted white (value=1) → fillStyle = '#d0d0d0' (light gray)
         - Painted black (value=2) → fillStyle = '#404040' (dark gray)
         - Unset (value=0) → skip overlay, let QR show through
       - Grid lines drawing will be added in this task but controlled via CSS class (see Task 2)
       - Protected area outlines will be added in Task 2

    4. Update `initPaintCanvas` to:
       - Remove paint-canvas initialization
       - Keep paintGrid creation and pattern restoration
       - Remove renderPaintGrid call (replaced by renderQRWithOverlay)

    5. Update corruption pipeline (`runCorruptionPipeline`) to:
       - Call renderQRWithOverlay instead of separate renders
       - Pass the QR canvas, model, paintGrid, and functionMask

    6. Update the QR generation flow (`generateQR` and related functions) to:
       - Use renderQRWithOverlay for initial display
       - Ensure functionMask is created and cached for painting

    Key constants to use (per locked decisions):
    - PAINTED_WHITE = '#d0d0d0' (light gray)
    - PAINTED_BLACK = '#404040' (dark gray)
    
    IMPORTANT: The overlay must be 100% opaque (no transparency). The colors are chosen to be distinguishable from pure black (#000000) and pure white (#ffffff) QR modules.
  </action>
  <verify>
    - Open index.html in browser
    - Generate a QR code
    - Verify the paint canvas is gone and there's only one QR display canvas
    - Use browser dev tools to confirm paint-canvas element no longer exists
  </verify>
  <done>
    Single unified canvas renders QR code base layer. Separate paint-canvas removed from DOM and JavaScript.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement protected area visualization and hover grid</name>
  <files>index.html</files>
  <action>
    Add protected area visualization and hover-triggered grid lines:

    1. Create CSS classes for hover state and cursor feedback:
       Add to CSS section:
       ```css
       .qr-canvas-container {
         position: relative;
         display: inline-block;
       }
       
       .qr-canvas-container.hovering canvas {
         /* Grid lines drawn via JS, this class triggers the render */
       }
       
       .qr-canvas-container.blocked {
         cursor: not-allowed;
       }
       
       .qr-canvas-container.paintable {
         cursor: crosshair;
       }
       ```

    2. Create `drawProtectedAreaOutlines(ctx, functionMask, gridSize, cellSize)`:
       - Iterate through functionMask array
       - For each protected cell (mask[idx] === true), draw border
       - Border style (Claude's discretion):
         - Suggested: strokeStyle = '#ff6b6b' (red), lineWidth = 2, setLineDash([4, 2])
       - Draw strokeRect around each protected cell
       - Reset lineDash after drawing

    3. Create `drawGridLines(ctx, gridSize, cellSize)`:
       - strokeStyle = '#e0e0e0' (very light gray)
       - lineWidth = 1
       - Draw vertical and horizontal lines at each cell boundary
       - This is called conditionally based on hover state

    4. Update `renderQRWithOverlay` to:
       - Accept `isHovering` parameter
       - Call drawProtectedAreaOutlines after painted overlay (Layer 3)
       - Call drawGridLines if isHovering is true (Layer 2 or 4)
       - Recommended layer order:
         - Layer 0: QR base
         - Layer 1: Painted overlay
         - Layer 2: Grid lines (if hovering)
         - Layer 3: Protected area outlines (always visible)

    5. Add hover detection to canvas-container:
       - Add mouseenter/mouseleave listeners to toggle 'hovering' CSS class
       - Re-render with grid lines when hovering
       - Remove grid lines when mouse leaves

    6. Create `isProtected(x, y, gridSize, functionMask)` helper:
       - Returns functionMask[y * gridSize + x] or false if out of bounds
       - Will be used by painting logic in 07-02

    7. Update `updateCursorForPosition(x, y)` function:
       - Check if position is protected using isProtected()
       - Add/remove 'blocked' or 'paintable' CSS class to container
       - Called during mousemove over canvas

    Implementation notes:
    - Protected area outlines are ALWAYS visible (not just on hover)
    - Grid lines are ONLY visible on hover
    - Cursor changes immediately when entering/leaving protected areas
    - All protected areas look the same (finder, timing, alignment patterns)
  </action>
  <verify>
    - Open index.html and generate QR code
    - Verify red border outlines appear around:
      - Top-left, top-right, bottom-left finder patterns (9x9 areas)
      - Timing patterns (row 6 and column 6)
      - Alignment patterns (if version >=2, small squares)
    - Hover over canvas and verify faint grid lines appear
    - Move mouse over protected area and verify cursor changes to not-allowed
    - Move mouse to editable area and verify cursor is crosshair
  </verify>
  <done>
    Protected areas have visible border outlines, cursor changes to not-allowed on protected areas, grid lines appear on hover.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update pattern persistence for new overlay format</name>
  <files>index.html</files>
  <action>
    Ensure pattern state persists correctly with the new overlay rendering:

    1. Verify paintGrid state model is unchanged:
       - PixelGrid class stores cells as 0/1/2 (unset/white/black)
       - This format is already compatible with overlay rendering
       - No changes needed to savePattern/loadPattern functions

    2. Update pattern restoration in initPaintCanvas:
       - After loading saved cells into paintGrid
       - Call renderQRWithOverlay to display restored pattern
       - Ensure functionMask is available before rendering

    3. Update clear button functionality:
       - paintGrid.clear() already sets all to 0
       - Ensure clear triggers renderQRWithOverlay re-render

    4. Add savePattern call after any painting operation:
       - Already wired in 06-01 (savePattern called after pointerdown)
       - Verify this still works after removing paint-canvas
       - Painting events will be updated in 07-02, but verify existing save still works

    5. Test edge cases:
       - Reload page with saved pattern: verify overlay renders correctly
       - Pattern on protected areas: should not be visible (protected areas show QR only)
       - Mixed pattern (some white, some black, some unset): verify correct colors

    Verification:
    - Paint some pixels, reload page, verify pattern restores correctly
    - Pattern should overlay on QR with correct light/dark gray colors
  </action>
  <verify>
    - npm test (if available)
    - Or manual test: paint pattern, save, reload, verify restoration works
    - Verify localStorage still contains pattern data in expected format
  </verify>
  <done>
    Pattern persistence continues to work with new overlay rendering. Saved patterns restore correctly on page reload.
  </done>
</task>

</tasks>

<verification>
Overall phase verification for 07-01:
1. Single canvas displays QR with painted overlay (not separate canvases)
2. Painted pixels are distinguishable from QR modules (light gray vs dark gray)
3. Protected areas have visible border outlines
4. Cursor changes to not-allowed on protected areas
5. Grid lines visible only on hover
6. Pattern persistence still works correctly
</verification>

<success_criteria>
- Paint canvas successfully removed, single unified QR canvas
- Overlay rendering with distinguishable colors (light gray #d0d0d0, dark gray #404040)
- Protected areas marked with border outlines
- Cursor feedback working (not-allowed on protected, crosshair elsewhere)
- Grid lines on hover only
- Pattern save/load continues to work
</success_criteria>

<output>
After completion, create `.planning/phases/07-painting-overhaul/07-01-SUMMARY.md`
</output>
